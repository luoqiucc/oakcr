import type {Metadata} from 'next'

export const metadata: Metadata = {
    title: '个人中心',
    description: '应用设置',
}

export default function ProfileSettings() {
    return (
        <>
        </>
    )
}
